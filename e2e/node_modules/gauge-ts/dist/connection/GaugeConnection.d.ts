/// <reference types="node" />
import { EventEmitter } from "events";
import { gauge } from '../gen/messages';
export declare class GaugeConnection extends EventEmitter {
    private _socket;
    private _host;
    constructor();
    start(): void;
    write(message: gauge.messages.IMessage): void;
    private messageHandler;
    private getGaugeInternalPort;
}
