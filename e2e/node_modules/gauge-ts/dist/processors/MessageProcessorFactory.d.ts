/// <reference types="node" />
import { EventEmitter } from "events";
import { gauge } from "../gen/messages";
import { StaticLoader } from "../loaders/StaticLoader";
import { IMessageProcessor } from "./IMessageProcessor";
export declare class MessageProcessorFactory extends EventEmitter {
    private _processors;
    private readonly _loader;
    constructor(loader: StaticLoader);
    get(messageType: gauge.messages.Message.MessageType): IMessageProcessor;
    process(message: gauge.messages.IMessage): Promise<void>;
    private _process;
}
