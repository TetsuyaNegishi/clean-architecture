import { gauge } from "../gen/messages";
import { StaticLoader } from "../loaders/StaticLoader";
import { IMessageProcessor } from "./IMessageProcessor";
export declare class CacheFileProcessor implements IMessageProcessor {
    private readonly _loader;
    constructor(loader: StaticLoader);
    process(message: gauge.messages.IMessage): Promise<gauge.messages.IMessage>;
    private loadFromDisk;
}
