import { Decorator, MethodDeclaration, Printer } from "typescript";
export declare abstract class CodeHelper {
    protected printer: Printer;
    protected getStepTexts(node: MethodDeclaration): Array<string>;
    protected isStepDecorator(d: Decorator): boolean;
    protected hasStepDecorator(node: MethodDeclaration): boolean;
    protected hasStepText(node: MethodDeclaration, stepText: string): any;
}
